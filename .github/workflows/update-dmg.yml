name: Update Latest DMG on Main for GitHub Pages

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  update-main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository on main
        uses: actions/checkout@v3
        with:
          ref: main

      - name: Set up jq
        run: sudo apt-get install -y jq

      - name: Get latest release asset URL
        id: get_asset
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Fetch the JSON for the latest release (with authentication)
          RELEASE_JSON=$(curl -s -H "Authorization: token $GITHUB_TOKEN" https://api.github.com/repos/${GITHUB_REPOSITORY}/releases/latest)
          # Extract the asset URL for "Willow.Installer.dmg"
          ASSET_URL=$(echo "$RELEASE_JSON" | jq -r '.assets[] | select(.name == "Willow.Installer.dmg") | .url')
          if [ -z "$ASSET_URL" ]; then
            echo "No asset named 'Willow.Installer.dmg' found"
            exit 1
          fi
          echo "asset_url=$ASSET_URL" >> $GITHUB_OUTPUT

      - name: Download the DMG asset
        run: |
          curl -L -H "Accept: application/octet-stream" "${{ steps.get_asset.outputs.asset_url }}" -o "Willow.Installer.dmg"

      - name: Move DMG to temporary location
        run: mv "Willow.Installer.dmg" /tmp/Willow.Installer.dmg

      - name: Copy DMG from TMP back to main branch
        run: |
          # Copy the file from /tmp back into the current directory
          cp /tmp/Willow.Installer.dmg "./Willow.Installer.dmg"
          git add "./Willow.Installer.dmg"

      - name: Commit and push update to main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git commit -m "Update latest DMG from release ${{ github.ref_name }}" || echo "No changes to commit"
          git push "https://${GITHUB_ACTOR}:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" main
